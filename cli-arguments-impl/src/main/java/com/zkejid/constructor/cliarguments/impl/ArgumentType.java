package com.zkejid.constructor.cliarguments.impl;

/**
 * Describes type of registered arguments. Plain arguments are not registered, so they have no type.
 */
enum ArgumentType {
  FLAG,
  PROPERTY
}
